import TopNavbar from "./topNavbar";
import BottomNavbar from "./bottomNavbar";

const Header = () => {
  return (
    <header className="">
      <TopNavbar />
      <BottomNavbar />
    </header>
  );
};

export default Header;
