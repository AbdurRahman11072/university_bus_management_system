import { ContactMessages } from "@/components/dashboard/contact";

import React from "react";

const page = () => {
  return (
    <div>
      <ContactMessages />
    </div>
  );
};

export default page;
